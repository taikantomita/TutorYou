import os, sys  # Multiple imports on one line, unused imports

def my_function():
    a=5+3   # Missing whitespace around operator
    if a > 10:
       print("Value is greater than 10") # Indentation issue and line too long

my_function()
